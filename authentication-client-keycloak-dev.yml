server:
  port: ${CUSTOM.SERVER.PORT:9099}

spring:
  security:
    oauth2:
      client:
        provider:
          mitocode-realm:
            issuer-uri: http://localhost:8080/realms/mitocode-realm
        registration:
          mitocode-realm:
            provider: mitocode-realm
            client-name: Mitocode Client Keycloack
            client-id: mitocode-client
            client-secret: tr3qqx2vu1mbTETJJJKBkCGXMCHxw92R
            scope: openid
            authorization-grant-type: authorization_code
